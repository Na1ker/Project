# macOS
.DS_Store

# Editors
.vscode/
.idea/

# Environment / secrets
.env
.env.local

# Dependencies / build (только в корне — не задевает .claude/skills/build)
node_modules/
.next/
/dist/
/build/
__pycache__/
*.pyc

# Local database (сделки, ключи API — никогда не коммитим)
data/
*.db
*.db-journal
*.db-wal
*.db-shm
